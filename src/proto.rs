//! Wire protocol between the client, daemon, and hook, plus the registry
//! snapshot serialize/parse. The socket message layer is added in the
//! integration phase; the registry record format is ported first.
